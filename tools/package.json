{
  "name": "@theokit/gateway-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Repository quality gates. Never published; the `private` flag is what keeps it that way.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "echo 'no TypeScript in tools/ \u2014 the gates are .mjs' && exit 0"
  },
  "devDependencies": {
    "vitest": "^4.1.8"
  }
}
